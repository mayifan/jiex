# 金额分配方式设计

## 当前方案（自动分配）
- 为每个次要参与者设置参与次数
- 系统自动将参与者分配到各个项目
- 每次固定金额或自定义金额

## 新方案（手动分配）

### 界面布局

```
┌─────────────────────────────────────────────────────────────┐
│  金额分配                                                    │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  项目1：XX项目名称                                           │
│  ┌──────────────────────────────────────────────────────┐  │
│  │ 项目总额：850元                                       │  │
│  │                                                       │  │
│  │ 主要参与者：马一帆 (SFD17221)                        │  │
│  │ 金额：650元 (自动计算 = 850 - 次要参与者金额之和)   │  │
│  │                                                       │  │
│  │ 次要参与者：                                         │  │
│  │ ┌────────────┬────────┬──────────────┐              │  │
│  │ │ 参与者      │ 选择   │ 金额(元)     │              │  │
│  │ ├────────────┼────────┼──────────────┤              │  │
│  │ │ 刘纪甲      │ ☑      │ [100____]    │              │  │
│  │ │ 丁少康      │ ☐      │ [100____]    │  (禁用)     │  │
│  │ │ 王鹏        │ ☑      │ [100____]    │              │  │
│  │ │ 盛泉铭      │ ☐      │ [100____]    │  (禁用)     │  │
│  │ │ 刘仲        │ ☐      │ [100____]    │  (禁用)     │  │
│  │ └────────────┴────────┴──────────────┘              │  │
│  │                                                       │  │
│  │ 次要参与者总额：200元                                │  │
│  │ 主要参与者金额：650元 ✓                             │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                              │
│  项目2：YY项目名称                                           │
│  ┌──────────────────────────────────────────────────────┐  │
│  │ ...                                                   │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

### 功能特性

1. **项目展示**
   - 显示项目名称（从Excel B4读取）
   - 显示项目总额（从C17或C18读取）

2. **主要参与者**
   - 显示选中的第一参与者姓名和工号
   - 金额自动计算 = 项目总额 - 次要参与者金额之和
   - 如果金额为负数，显示红色警告

3. **次要参与者选择**
   - 所有其他参与者显示为复选框列表
   - 勾选后启用金额输入框
   - 未勾选时金额输入框禁用且清空
   - 默认金额：100元

4. **验证规则**
   - 每个项目至少选择2个不同的次要参与者
   - 次要参与者金额之和不能超过项目总额
   - 主要参与者金额必须 ≥ 0

5. **数据结构**
```javascript
// 项目分配数据
projectAllocations = {
  [fileUid]: {
    projectName: 'XX项目',
    totalAmount: 850,
    primaryParticipant: {
      code: 'SFD17221',
      name: '马一帆',
      amount: 650  // 自动计算
    },
    secondaryParticipants: [
      { code: 'SDF21054', name: '刘纪甲', amount: 100 },
      { code: 'SFD21724', name: '王鹏', amount: 100 }
    ]
  }
}
```

### 优势
1. ✅ 更直观：可以直接看到每个项目的分配情况
2. ✅ 更灵活：可以为不同项目分配不同的参与者和金额
3. ✅ 更精确：金额可以单独调整，不受"次数"限制
4. ✅ 实时验证：立即看到主要参与者的金额是否合理

### 实现要点

1. **展开/折叠项目**（可选）
   - 如果项目很多，可以添加展开/折叠功能
   - 默认只展开第一个项目

2. **复制分配**（可选）
   - 添加"复制到其他项目"按钮
   - 快速将当前项目的分配方案应用到其他项目

3. **统计汇总**（可选）
   - 在顶部显示每个参与者的总参与次数和总金额
   - 帮助平衡参与者之间的分配
