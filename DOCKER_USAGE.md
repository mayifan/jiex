# Docker 镜像使用说明

## 📦 镜像信息

- **镜像文件**: `jiex-app.tar`
- **文件大小**: 51MB
- **文件位置**: `/Users/mayifan/Documents/work/JiexGit/jiex-app.tar`

## 🚀 使用步骤

### 方法一：加载并运行 Docker 镜像（推荐）

#### 1. 加载镜像
在收到 `jiex-app.tar` 文件后，在终端运行：

```bash
docker load -i jiex-app.tar
```

输出示例：
```
Loaded image: jiex-app:latest
```

#### 2. 运行容器
```bash
docker run -d -p 8080:80 --name jiex-app jiex-app:latest
```

#### 3. 访问应用
打开浏览器访问：**http://localhost:8080**

#### 4. 停止容器
```bash
docker stop jiex-app
```

#### 5. 启动容器
```bash
docker start jiex-app
```

#### 6. 删除容器
```bash
docker stop jiex-app
docker rm jiex-app
```

---

### 方法二：使用 Docker Compose（可选）

如果你同时收到了项目文件夹，可以使用 Docker Compose：

```bash
cd /path/to/JiexGit
docker-compose up -d
```

访问地址：**http://localhost:8080**

---

## 🛠️ 常用命令

### 查看运行中的容器
```bash
docker ps
```

### 查看所有容器（包括停止的）
```bash
docker ps -a
```

### 查看容器日志
```bash
docker logs jiex-app
```

### 进入容器
```bash
docker exec -it jiex-app sh
```

### 重启容器
```bash
docker restart jiex-app
```

---

## 🔧 端口配置

默认端口映射：`8080:80`

如需修改端口，运行容器时指定：
```bash
docker run -d -p 3000:80 --name jiex-app jiex-app:latest
```
然后访问：**http://localhost:3000**

---

## ⚠️ 注意事项

1. **确保 Docker 已安装**
   - Mac: 安装 Docker Desktop
   - Windows: 安装 Docker Desktop
   - Linux: 安装 Docker Engine

2. **确保端口未被占用**
   - 检查 8080 端口是否可用
   - 如被占用，使用其他端口（如 3000, 5000）

3. **首次运行可能较慢**
   - Docker 需要解压镜像
   - 请耐心等待

---

## 📱 应用功能

### 1. 上传 Excel 文件
- 支持 .xlsx 和 .xls 格式
- 可批量上传多个文件

### 2. 参与者管理
- 手动新增参与者（姓名 + 工号）
- 删除不需要的参与者
- 选择第一参与者（默认马一帆）

### 3. 金额分配
- 每个项目固定 850 元
- 第一参与者固定 650 元
- 其他 2 人共 200 元
- 按百元为单位分配

### 4. 生成文档
- 自动生成结项单 Word 文档
- 打包为 ZIP 文件下载

---

## 🆘 故障排查

### 问题：容器无法启动
```bash
# 查看日志
docker logs jiex-app

# 删除并重新运行
docker rm jiex-app
docker run -d -p 8080:80 --name jiex-app jiex-app:latest
```

### 问题：端口被占用
```bash
# Mac/Linux 查看端口占用
lsof -i :8080

# 使用其他端口
docker run -d -p 9000:80 --name jiex-app jiex-app:latest
```

### 问题：无法访问应用
1. 确认容器正在运行：`docker ps`
2. 确认端口映射正确
3. 尝试访问：`http://localhost:8080`
4. 检查防火墙设置

---

## 📞 技术支持

如有问题，请提供以下信息：
1. Docker 版本：`docker --version`
2. 容器状态：`docker ps -a`
3. 容器日志：`docker logs jiex-app`
4. 错误截图

---

## 🎉 快速测试

加载并运行镜像的完整命令：
```bash
# 1. 加载镜像
docker load -i jiex-app.tar

# 2. 运行容器
docker run -d -p 8080:80 --name jiex-app jiex-app:latest

# 3. 打开浏览器
# 访问 http://localhost:8080
```

一切顺利的话，你将看到应用界面！🚀
